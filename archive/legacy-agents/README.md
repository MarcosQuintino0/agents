# Legacy Agents Archive

Esta pasta preserva os agentes originais antes da migracao para a skill `qa-api-testing`.

Ela nao e a fonte ativa da skill. A skill ativa fica em:

```text
skill/qa-api-testing/
```

Estes arquivos servem apenas para auditoria, comparacao e recuperacao de conteudo. Nao publique esta pasta no pacote NPM e nao use estes agentes como fluxo principal quando estiver evoluindo a skill.
